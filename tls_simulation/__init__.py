# tls_simulation — Classical TLS Reference Implementation (RSA-2048 + ECDHE-P256)
# NOTE: All cryptographic operations use the real `cryptography` library, not mocks.
# This module exists for benchmarking comparison against KEMTLS, not as a "simulation".
